---
import DefaultLayout from '@layouts/DefaultLayout.astro'
import PageHeader from '@components/PageHeader.astro'
import { Icon } from 'astro-icon/components'
import { localizePath } from 'src/i18n/i18n'
import { getModule } from 'src/data/modules'
import type { ModuleId } from 'src/data/modules'
import { modulePartsData, mergeParts } from 'src/data/moduleParts'
import type { TranslationPart, TeachersGuide, RelatedModuleCard, MergedPart, MergedMaterial, MergedActivityStep, PartMeta } from 'src/data/moduleParts'
import { useTranslations } from 'src/i18n/index'

const locale = Astro.currentLocale
const { t, v } = useTranslations(locale)

const objectives = v<ReadonlyArray<string>>('contentDC.objectives') ?? []
const outcomes = v<ReadonlyArray<string>>('contentDC.outcomes') ?? []
const teachersGuide = v<TeachersGuide>('contentDC.teachersGuide') ?? {}
const currentModule = getModule('dc')!
// mergeParts combines structural asset data (file paths, video sources) from
// modulePartsData with the locale-specific text from contentDC.parts. The
// result is what the template consumes — neither source alone is sufficient.
const parts = mergeParts('dc', v<ReadonlyArray<TranslationPart>>('contentDC.parts') ?? [])

// Related module cards are authored in the translation file but their counts
// (parts, materials, videos, etc.) are overwritten here with live values from
// modulePartsData. This keeps cards accurate when a module gains new content
// without requiring editors to update every locale's translation file.
const relatedModuleCards = (v<ReadonlyArray<RelatedModuleCard>>('contentDC.relatedModuleCards') ?? []).map(card => { const mod = getModule(card.moduleId); if (!mod) return card; return { ...card, parts: modulePartsData[mod.id].length, materials: mod.materials, videos: mod.videos, challenges: mod.challenges, games: mod.games } })

const hasCount = (value: unknown): value is number => typeof value === 'number' && value > 0

const formatCount = (count: number, singularKey: string, pluralKey: string) =>
  `${count} ${t(count === 1 ? singularKey : pluralKey)}`

const getPartTitle = (part: MergedPart) => part.titleKey ? t(part.titleKey) : ''

const moduleImages = {
  dc: '/images/learning-hub/01_digital-citizenship.png',
  ap: '/images/learning-hub/02_attacker-perspective.png',
  at: '/images/learning-hub/03_authentication.png',
  dp: '/images/learning-hub/04_data-privacy.png',
  se: '/images/learning-hub/05_social-engineering.png',
  mw: '/images/learning-hub/06_malware.png',
  da: '/images/learning-hub/07_digital-abuse.png',
}
---

<DefaultLayout title={t('modules.dc.title')}>
  <PageHeader
      title={t('modules.dc.title')}
      subtitle={t('contentDC.subtitle')}
      underlineColor="var(--brand-DC)"
      bgType="module"
      buttons={[
        {
          text: t('contentPage.downloadGuide'),
          href: currentModule.teachersGuide.href,
          variant: "primary",
          icon: "lucide:download",
          download: teachersGuide.download ?? '',
        },
        {
          text: t('contentPage.downloadAll'),
          href: "/materials/digital-citizenship/digital-citizenship.zip",
          variant: "primary",
          icon: "lucide:download",
          download: "Digital Citizenship Material Package",
        },
      ]}
      />

  <!-- Main Content Area -->
  <section class="my-12">
    <div class="container">

      <!-- Learning Overview: Aim / Objectives / Outcomes tabs -->
      <div class="aoo-card">
        <div class="dc-tabs">
          <div class="dc-tabs__bar" role="tablist">
            <button class="dc-tab" role="tab" aria-selected="true" data-panel="aoo-aim">
              <img class="dc-tab-img-icon" src="/images/learning-hub/aim.png" alt="" aria-hidden="true" />
              {t('contentPage.aimTitle')}
            </button>
            <button class="dc-tab" role="tab" aria-selected="false" data-panel="aoo-objectives">
              <img class="dc-tab-img-icon" src="/images/learning-hub/objectives.png" alt="" aria-hidden="true" />
              {t('contentPage.objectivesTitle')}
              <span class="dc-tab__badge">{objectives.length}</span>
            </button>
            <button class="dc-tab" role="tab" aria-selected="false" data-panel="aoo-outcomes">
              <img class="dc-tab-img-icon" src="/images/learning-hub/outcomes.png" alt="" aria-hidden="true" />
              {t('contentPage.outcomesTitle')}
              <span class="dc-tab__badge">{outcomes.length}</span>
            </button>
          </div>

          <div class="dc-tab-panel" role="tabpanel" id="aoo-aim">
            <div class="dc-aim-content">
              <p class="dc-aim-text">{t('contentDC.aim')}</p>
            </div>
          </div>

          <div class="dc-tab-panel" role="tabpanel" id="aoo-objectives" hidden>
            <ul class="dc-step-list">
              {objectives.map((obj, i) => (
                <li class="dc-step-item dc-step-item--plain">
                  <span class="dc-step-num">{String(i + 1).padStart(2, '0')}</span>
                  <p class="dc-step-title">{obj}</p>
                </li>
              ))}
            </ul>
          </div>

          <div class="dc-tab-panel" role="tabpanel" id="aoo-outcomes" hidden>
            <ul class="dc-step-list">
              {outcomes.map((outcome, i) => (
                <li class="dc-step-item dc-step-item--plain">
                  <span class="dc-step-num">{String(i + 1).padStart(2, '0')}</span>
                  <p class="dc-step-title">{outcome}</p>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>

      <!-- Section Divider -->
      <div class="dc-divider dc-divider--center" style="margin-top: var(--space-xl);">What's inside</div>

      <!-- Module Content -->
      <div class="dc-section" id="module-content">
        <div class="dc-section__header">
          <h2>{t('contentPage.moduleContent')}</h2>
          <p class="dc-section__subtitle">{t('contentPage.moduleContentSubtitle')}</p>
        </div>
        <div class="dc-module-list">
          <div class="dc-module-row">
            <a class="dc-part-button" href="#teachers-guide">{t('contentPage.guideLabel')}</a>
            <div class="dc-teaching-guide" id="teachers-guide">
              <div class="dc-package-banner__copy">
                <h3>{t('contentPage.teachersGuideTitle')}</h3>
                <p class="dc-package-banner__sub">{t('contentPage.guideDescription')}</p>
              </div>
              <div class="dc-package-btn-wrap">
                <a
                  class="button has-icon dc-package-btn"
                  href={currentModule.teachersGuide.href}
                  download={teachersGuide.download ?? ''}
                  aria-label={String(teachersGuide.ariaLabel ?? '')}
                >
                  <Icon name="lucide:download" />
                  {t('contentPage.teachersGuideDownload')}
                </a>
              </div>
            </div>
          </div>

          {parts.map((part) => {
            const meta: PartMeta = part.meta ?? { steps: 0, materials: 0, videos: 0 }
            const included = part.included ?? {}
            const partMaterials = included.materials ?? []
            const partActivity = included.activityPlan ?? []
            const featuredVideo = part.featuredVideo
            const title = getPartTitle(part)
            // Activity plan opens first when present — it is the primary teaching
            // sequence. Materials are secondary. Video is the fallback tab.
            const firstPanel = partActivity.length ? 'activity' : partMaterials.length ? 'materials' : 'video'

            return (
              <div class="dc-module-row">
                <a class="dc-part-button" href={`#${part.anchorId}`}>{t('hub.partLabel')} {part.number}</a>
                <details class="dc-module" id={part.anchorId}>
                  <summary class="dc-module__header">
                    <div class="dc-module__copy">
                      <h3>{title}</h3>
                      <div class="dc-module__meta">
                        {hasCount(meta.steps) ? (
                          <span class="dc-meta-item"><Icon name="lucide:list-checks" /> {formatCount(meta.steps, 'contentPage.stepSingular', 'contentPage.stepPlural')}</span>
                        ) : null}
                        {hasCount(meta.materials) ? (
                          <span class="dc-meta-item"><Icon name="lucide:file-text" /> {formatCount(meta.materials, 'contentPage.materialSingular', 'contentPage.materialPlural')}</span>
                        ) : null}
                        {hasCount(meta.videos) ? (
                          <span class="dc-meta-item"><Icon name="lucide:play-circle" /> {formatCount(meta.videos, 'contentPage.videoSingular', 'contentPage.videoPlural')}</span>
                        ) : null}
                      </div>
                      <p class="dc-goal"><b>{t('contentPage.goalLabel')}</b> {String(part.goal ?? '')}</p>
                    </div>
                    <span class="dc-module__toggle" aria-hidden="true">
                      <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
                        <path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </span>
                  </summary>
                  <div class="dc-module__content">
                    <div class="dc-package-banner">
                      <div class="dc-package-banner__copy">
                        <p class="dc-package-banner__title">{t('contentPage.materialPackage')}</p>
                        <p class="dc-package-banner__sub">{t('contentPage.materialPackageSub')}</p>
                      </div>
                      <div class="dc-package-btn-wrap">
                        <a
                          class="button has-icon dc-package-btn"
                          href={String(part.bundle?.href ?? '')}
                          download={String(part.bundle?.filename ?? '')}
                        >
                          <Icon name="lucide:download" />
                          {t('contentPage.downloadPart')}
                        </a>
                      </div>
                    </div>

                    {(partMaterials.length || partActivity.length || featuredVideo) ? (
                      <div class="dc-tabs">
                        <div class="dc-tabs__bar" role="tablist">
                          {partActivity.length ? (
                            <button
                              class="dc-tab"
                              role="tab"
                              aria-selected={firstPanel === 'activity' ? 'true' : 'false'}
                              data-panel={`activity-${part.anchorId}`}
                            >
                              <Icon name="lucide:list" />
                              {t('contentPage.activityPlan')}
                              <span class="dc-tab__badge">{partActivity.length}</span>
                            </button>
                          ) : null}
                          {partMaterials.length ? (
                            <button
                              class="dc-tab"
                              role="tab"
                              aria-selected={firstPanel === 'materials' ? 'true' : 'false'}
                              data-panel={`materials-${part.anchorId}`}
                            >
                              <Icon name="lucide:file-text" />
                              {t('contentPage.materialPlural')}
                              <span class="dc-tab__badge">{partMaterials.length}</span>
                            </button>
                          ) : null}
                          {featuredVideo ? (
                            <button
                              class="dc-tab"
                              role="tab"
                              aria-selected={firstPanel === 'video' ? 'true' : 'false'}
                              data-panel={`video-${part.anchorId}`}
                            >
                              <Icon name="lucide:video" />
                              {t('contentPage.videoPlural')}
                              <span class="dc-tab__badge">1</span>
                            </button>
                          ) : null}
                        </div>

                        {partMaterials.length ? (
                          <div
                            class="dc-tab-panel"
                            role="tabpanel"
                            id={`materials-${part.anchorId}`}
                            hidden={firstPanel !== 'materials'}
                          >
                            <ul class="dc-step-list">
                              {partMaterials.map((item: MergedMaterial, i: number) => (
                                <li class="dc-step-item">
                                  <span class="dc-step-num">{String(i + 1).padStart(2, '0')}</span>
                                  <p class="dc-step-title"><b>{String(item.kind ?? '')}:</b> {String(item.name ?? '')}</p>
                                  <a
                                    class="dc-item-download"
                                    href={String(item.href ?? '')}
                                    download={String(item.filename ?? '')}
                                    aria-label={String(item.ariaLabel ?? '')}
                                  >
                                    <Icon name="lucide:download" />
                                    <span>{t('contentPage.download')}</span>
                                  </a>
                                </li>
                              ))}
                            </ul>
                          </div>
                        ) : null}

                        {partActivity.length ? (
                          <div
                            class="dc-tab-panel"
                            role="tabpanel"
                            id={`activity-${part.anchorId}`}
                            hidden={firstPanel !== 'activity'}
                          >
                            <ul class="dc-step-list">
                              {partActivity.map((step: MergedActivityStep) => (
                                <li class="dc-step-item dc-step-item--plain">
                                  <span class="dc-step-num">{String(step.stepNumber ?? 0).padStart(2, '0')}</span>
                                  <p class="dc-step-title"><b>{t('contentPage.stepSingular')} {String(step.stepNumber ?? '')}:</b> {String(step.title ?? '')}</p>
                                </li>
                              ))}
                            </ul>
                          </div>
                        ) : null}

                        {featuredVideo ? (
                          <div
                            class="dc-tab-panel"
                            role="tabpanel"
                            id={`video-${part.anchorId}`}
                            hidden={firstPanel !== 'video'}
                          >
                            <div class="dc-video-preview">
                              <div class="dc-video-preview__thumb">
                                <video
                                  controls
                                  preload="metadata"
                                  poster={String(featuredVideo.posterSrc ?? '')}
                                >
                                  <source src={String(featuredVideo.videoSrc ?? '')} />
                                  {(featuredVideo.tracks ?? []).map((track) => (
                                    <track
                                      label={String(track.label ?? '')}
                                      kind="subtitles"
                                      srclang={String(track.srclang ?? '')}
                                      src={String(track.src ?? '')}
                                    />
                                  ))}
                                </video>
                              </div>
                              <div class="dc-video-preview__info">
                                <h4 class="dc-video-preview__title">{String(featuredVideo.title ?? '')}</h4>
                                <p class="dc-video-preview__desc">{String(featuredVideo.supportText ?? '')}</p>
                                <div class="dc-video-preview__actions">
                                  <div class="dc-video-action-wrap">
                                    <a
                                      class="dc-item-download"
                                      href={String(featuredVideo.downloads?.video?.href ?? '')}
                                      download={String(featuredVideo.downloads?.video?.filename ?? '')}
                                      aria-label={String(featuredVideo.downloads?.video?.ariaLabel ?? '')}
                                    >
                                      <Icon name="lucide:download" />
                                      <span>{t('contentPage.videoSingular')}</span>
                                    </a>
                                  </div>
                                  <div class="dc-video-action-wrap">
                                    <a
                                      class="dc-item-download"
                                      href={String(featuredVideo.downloads?.subtitles?.href ?? '')}
                                      download={String(featuredVideo.downloads?.subtitles?.filename ?? '')}
                                      aria-label={String(featuredVideo.downloads?.subtitles?.ariaLabel ?? '')}
                                    >
                                      <Icon name="lucide:captions" />
                                      <span>{t('contentPage.subtitlesSingular')}</span>
                                    </a>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </div>
                        ) : null}
                      </div>
                    ) : null}
                  </div>
                </details>
              </div>
            )
          })}
        </div>
      </div>

      <!-- Section Divider -->
      <div class="dc-divider dc-divider--center">More resources</div>

      <!-- Related Modules -->
      <div class="dc-section">
        <div class="dc-section__header">
          <h2>{t('contentPage.relatedModules')}</h2>
          <p class="dc-section__subtitle">{t('contentPage.relatedModulesSubtitle')}</p>
        </div>

        <div class="dc-rm-list">
          {relatedModuleCards.map((card, idx) => {
            const moduleId = String(card.moduleId ?? '')
            const title = t(`modules.${moduleId}.title`)
            const href = localizePath(locale, String(card.href ?? ''))
            const image = moduleImages[moduleId as keyof typeof moduleImages]
            const brandVar = `var(--brand-${String(card.brand ?? '')})`

            return (
              <a
                class={`dc-rm-row${idx === 0 ? ' dc-rm-row--first' : ''}`}
                href={href}
                style={`--rm-color: ${brandVar};`}
              >
                <div class="dc-rm-icon">
                  <img src={image} alt="" decoding="async" loading="eager" />
                </div>
                <div class="dc-rm-body">
                  <strong class="dc-rm-title">{title}</strong>
                  <span class="dc-rm-desc">{String(card.description ?? '')}</span>
                </div>
                <div class="dc-rm-meta" aria-label={title}>
                  {hasCount(card.parts) ? (
                    <span class="dc-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
                      {card.parts}
                    </span>
                  ) : null}
                  {hasCount(card.materials) ? (
                    <span class="dc-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="8" y1="13" x2="16" y2="13"/><line x1="8" y1="17" x2="16" y2="17"/></svg>
                      {card.materials}
                    </span>
                  ) : null}
                  {hasCount(card.videos) ? (
                    <span class="dc-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
                      {card.videos}
                    </span>
                  ) : null}
                  {hasCount(card.challenges) ? (
                    <span class="dc-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
                      {card.challenges}
                    </span>
                  ) : null}
                  {hasCount(card.games) ? (
                    <span class="dc-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 12h4M8 10v4"/><circle cx="15" cy="11" r="0.8" fill="currentColor" stroke="none"/><circle cx="17" cy="13" r="0.8" fill="currentColor" stroke="none"/><path d="M2 9a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v1c0 1-0.5 2-1 2.5L19 17a3 3 0 0 1-2.5 1.5h-9A3 3 0 0 1 5 17l-2-4.5C2.5 12 2 11 2 10V9z"/></svg>
                      {card.games}
                    </span>
                  ) : null}
                </div>
                <span class="dc-rm-arrow" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none">
                    <path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </span>
              </a>
            )
          })}
        </div>
      </div>
    </div>
  </section>
</DefaultLayout>

<script>
  // When a URL contains a hash that targets a <details> element (e.g. a direct
  // link to a module part like /content#digital-environments), open it and
  // scroll to it. requestAnimationFrame gives the browser one paint cycle to
  // render the newly open details before scrollIntoView fires — without it the
  // element may not yet have layout and scrolling lands in the wrong position.
  const expandFromHash = () => {
    const hash = window.location.hash.slice(1)
    if (!hash) return

    const target = document.getElementById(hash)
    if (target instanceof HTMLDetailsElement) {
      target.open = true
      requestAnimationFrame(() => {
        target.scrollIntoView({ behavior: 'smooth', block: 'start' })
      })
    }
  }

  // astro:page-load fires on every view-transition navigation, so both handlers
  // must be re-registered here to stay active after client-side routing.
  document.addEventListener('astro:page-load', expandFromHash)
  expandFromHash()

  const initTabs = () => {
    document.querySelectorAll('.dc-tabs').forEach(tabs => {
      const bar = tabs.querySelector('.dc-tabs__bar')
      if (!bar) return
      bar.addEventListener('click', e => {
        const btn = (e.target as Element).closest('[role="tab"]') as HTMLElement | null
        if (!btn) return
        const targetId = btn.dataset.panel
        if (!targetId) return
        bar.querySelectorAll('[role="tab"]').forEach(t => {
          t.setAttribute('aria-selected', t === btn ? 'true' : 'false')
        })
        tabs.querySelectorAll('[role="tabpanel"]').forEach(p => {
          (p as HTMLElement).hidden = p.id !== targetId
        })
      })
    })
  }

  document.addEventListener('astro:page-load', initTabs)
  initTabs()
</script>

<style>
  .dc-grid {
    display: grid;
    gap: var(--space-l);
  }

  .dc-grid--two {
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  }

  .dc-grid--three {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .dc-grid--four {
    grid-template-columns: 1fr;
    gap: var(--space-m);
  }

  :global(.dc-panel) {
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    border: 2px solid #dbe2ea;
    border-radius: var(--radius-l);
    padding: var(--space-l);
    box-shadow: var(--elevation-1);
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  }

  .dc-panel:hover,
  .dc-panel:focus-within {
    transform: translateY(-4px);
    box-shadow: var(--elevation-3);
    border: 2px solid color-mix(in oklab,var(--brand-DC),white 60%);
  }

  .dc-grid--learning-overview {
    align-items: stretch;
  }

  .dc-panel--compact {
    padding: var(--space-m);
    display: flex;
    flex-direction: column;
  }

  .dc-panel--learning {
    height: 100%;
  }

  .dc-panel--objectives {
    background: linear-gradient(165deg, color-mix(in oklab, var(--color-neutral-100), white 42%), color-mix(in oklab, var(--color-neutral-300), white 28%));
  }

  .dc-panel--outcomes {
    background: linear-gradient(165deg, color-mix(in oklab, var(--color-neutral-100), white 40%), color-mix(in oklab, var(--color-neutral-300), white 26%));
  }

  .dc-panel--learning h3 {
    margin: 0;
    min-height: 2.6rem;
    display: flex;
    align-items: flex-start;
    font-size: clamp(1.35rem, 1vw + 1rem, 1.9rem);
    line-height: 1.1;
  }

  .dc-panel__content {
    margin-top: var(--space-xs);
    border-top: 2px solid color-mix(in oklab, var(--color-neutral-500), white 35%);
    padding-top: var(--space-s);
  }

  .dc-list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0;
  }

  .dc-list li {
    border: 0;
    border-radius: 0;
    background: transparent;
    padding: 0;
    font-size: var(--font-size--1);
    line-height: 1.5;
  }

  .dc-list li + li {
    margin-top: var(--space-s);
    padding-top: var(--space-s);
    border-top: 1px solid color-mix(in oklab, var(--color-neutral-500), white 55%);
  }

  .dc-aim-text {
    margin: 0;
    line-height: 1.32;
    font-size: var(--text-xl);
    color: var(--foreground-color);
  }

  .dc-section {
    margin-top: var(--space-xl);
  }

  .dc-section__header {
    display: grid;
    gap: var(--space-2xs);
    margin-bottom: var(--space-l);
  }

  .dc-section__subtitle {
    color: var(--foreground-color);
  }

  .dc-module-list {
    --dc-part-column: clamp(88px, 11vw, 120px);
    --dc-part-gap: var(--space-l);
    --dc-part-button-height: 2.8rem;
    display: grid;
    gap: var(--dc-part-gap);
  }

  .dc-module-row {
    position: relative;
    display: grid;
    grid-template-columns: var(--dc-part-column) minmax(0, 1fr);
    gap: var(--space-m);
    align-items: start;
  }

  .dc-module-row:not(:last-child)::after {
    content: '';
    position: absolute;
    left: calc(var(--dc-part-column) / 2);
    top: calc(var(--dc-part-button-height) + var(--space-2xs));
    height: calc(100% + var(--dc-part-gap) - var(--dc-part-button-height) - var(--space-s));
    border-left: 2px dashed color-mix(in oklab, var(--brand-DC), white 50%);
    transform: translateX(-50%);
    pointer-events: none;
  }

  .dc-part-button {
    min-height: var(--dc-part-button-height);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    padding: 0 var(--space-s);
    border: 2px solid var(--brand-DC);
    border-radius: 0.5rem;
    background: var(--brand-DC);
    color: #fff;
    box-shadow: var(--elevation-1);
    text-decoration: none;
    font-size: var(--font-size--3);
    font-weight: 800;
    line-height: 1;
    white-space: nowrap;
    position: relative;
    z-index: 1;
    transition: transform 0.2s ease, background-color 0.2s ease, color 0.2s ease;
  }

  .dc-part-button:hover {
    background: transparent;
    color: var(--foreground-color);
    transform: translateY(-1px);
  }

  .dc-part-button:focus-visible {
    outline: 2px solid color-mix(in oklab, var(--brand-DC), black 20%);
    outline-offset: 2px;
  }

  :global(.dc-module) {
    border: 2px solid color-mix(in oklab, var(--brand-DC), white 60%);
    border-radius: 1.25rem;
    padding: 0;
    background: linear-gradient(155deg, color-mix(in oklab, var(--brand-DC), white 92%), #ffffff 55%);
    box-shadow: var(--elevation-2);
    margin-bottom: 0;
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .dc-module:hover {
    transform: translateY(-3px);
    box-shadow: var(--elevation-3);
  }

  .dc-module__header {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-m);
    align-items: center;
    list-style: none;
    cursor: pointer;
    padding: clamp(1rem, 2vw, 1.75rem);
  }

  .dc-module__header::-webkit-details-marker {
    display: none;
  }

  .dc-module__header::marker {
    content: '';
  }

  .dc-module__header h3 {
    margin: 0;
    font-size: clamp(1.6rem, 1.3rem + 1vw, 2.4rem);
    line-height: 1.1;
    font-weight: 900;
    text-decoration: underline;
    text-decoration-color: var(--brand-DC);
    text-underline-offset: 0.35rem;
    text-decoration-thickness: 0.15rem;
  }

  .dc-module__header h3 a {
    color: var(--color-neutral-900);
    text-decoration: none;
  }

  .dc-module__header h3 a:hover,
  .dc-module__header h3 a:focus-visible {
    color: var(--brand-DC);
  }

  .dc-module__toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    border-radius: 50%;
    border: 2px solid color-mix(in oklab, var(--brand-DC), white 60%);
    background: color-mix(in oklab, var(--brand-DC), white 86%);
    color: var(--brand-DC);
    pointer-events: none;
    transition: background 0.2s ease, border-color 0.2s ease;
  }

  .dc-module__header:hover .dc-module__toggle {
    background: var(--brand-DC);
    border-color: var(--brand-DC);
    color: #ffffff;
  }

  .dc-module__toggle svg {
    width: 1.1rem;
    height: 1.1rem;
    transition: transform 0.3s ease;
  }

  .dc-module:not([open]) .dc-module__toggle svg {
    transform: rotate(0deg);
  }

  .dc-module[open] .dc-module__toggle svg {
    transform: rotate(90deg);
  }

  .dc-module__content {
    padding: 0 clamp(1rem, 2vw, 1.75rem) clamp(1rem, 2vw, 1.75rem);
  }

  .dc-goal {
    font-size: 1.05rem;
    line-height: 1.5;
    margin-top: var(--space-xs);
    color: var(--foreground-color);
    max-width: 70ch;
  }

  .dc-module__meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: var(--space-s);
  }

  .dc-meta-item {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--foreground-color);
  }

  .dc-meta-item [data-icon] {
    width: 1.1rem;
    height: 1.1rem;
    flex-shrink: 0;
    color: var(--brand-DC);
  }

  .dc-module__download {
    margin-top: var(--space-m);
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-s);
    align-items: center;
    justify-content: space-between;
    padding: var(--space-m);
    border-radius: var(--radius-m);
    border: 2px dashed var(--brand-DC);
    background: var(--background-color);
  }

  .dc-module__download-button {
    width: clamp(190px, 28%, 320px);
    border-radius: 10px;
    justify-content: center;
    background: var(--brand-DC);
    color: #fff;
    border: 2px solid color-mix(in oklab, var(--brand-DC), black 10%);
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .dc-module__download-button:hover {
    background: transparent;
    color: var(--brand-DC);
    transform: translateY(-1px);
  }

  .dc-module__download p {
    margin: 0;
    font-weight: 400;
    width: min(100%, 560px);
  }

  .dc-teaching-guide {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-xl);
    padding: var(--space-m) var(--space-l);
    border-radius: 1.25rem;
    background: color-mix(in oklab, var(--brand-DC), white 88%);
    border: 1.5px solid color-mix(in oklab, var(--brand-DC), white 60%);
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .dc-teaching-guide:hover {
    transform: translateY(-3px);
    box-shadow: var(--elevation-3);
  }

  .dc-teaching-guide h3 {
    margin: 0;
    font-size: clamp(1.6rem, 1.3rem + 1vw, 2.4rem);
    line-height: 1.1;
    font-weight: 900;
    text-decoration: underline;
    text-decoration-color: var(--brand-DC);
    text-underline-offset: 0.35rem;
    text-decoration-thickness: 0.15rem;
  }

  /* ── Package Banner ───────────────────────────────────────────────────────── */
  .dc-package-banner {
    margin-top: var(--space-m);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-s);
    padding: var(--space-m) var(--space-l);
    border-radius: var(--radius-m);
    background: color-mix(in oklab, var(--brand-DC), white 88%);
    border: 1.5px solid color-mix(in oklab, var(--brand-DC), white 60%);
    overflow: hidden;
  }

  .dc-package-banner__copy {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .dc-package-banner__title {
    margin: 0;
    font-weight: 700;
    font-size: var(--font-size-1);
    color: var(--color-neutral-900);
  }

  .dc-package-banner__sub {
    margin-top: var(--space-xs);
    font-size: var(--font-size-0);
    color: var(--foreground-color);
  }

  .dc-package-btn-wrap {
    flex-shrink: 0;
  }

  .dc-package-btn {
    border-radius: 10px;
    background: var(--brand-DC);
    color: #fff;
    border: 2px solid color-mix(in oklab, var(--brand-DC), black 10%);
    transition: background-color 0.2s ease, transform 0.2s ease;
  }

  .dc-package-btn:hover {
    background: color-mix(in oklab, var(--brand-DC), black 12%);
    color: #fff;
    transform: translateY(-1px);
  }

  /* ── AoO Overview Card ──────────────────────────────────────────────────── */
  .aoo-card {
    display: flex;
    flex-direction: column;
    padding: 1.25rem;
    border: 1px solid #dbe2ea;
    border-radius: 1rem;
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    box-shadow: var(--elevation-1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    margin-bottom: var(--space-l);
  }

  .aoo-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--elevation-3);
  }

  :global(html.darkmode) .aoo-card {
    background: linear-gradient(180deg, var(--dm-surface-2) 0%, var(--dm-surface-1) 100%);
    border-color: var(--dm-border-sub);
  }

  .aoo-card > .dc-tabs {
    margin-top: 0;
  }

  /* ── Tabs ─────────────────────────────────────────────────────────────────── */
  .dc-tabs {
    margin-top: var(--space-m);
  }

  .dc-tabs__bar {
    display: flex;
    gap: 0;
    flex-wrap: nowrap;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    border-bottom: 2px solid color-mix(in oklab, var(--brand-DC), white 70%);
    margin-bottom: var(--space-m);
    padding-right: var(--space-2xs);
  }

  .dc-tabs__bar::-webkit-scrollbar {
    display: none;
  }

  .dc-tab {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.55rem 1rem;
    background: none;
    border: none;
    border-bottom: 3px solid transparent;
    margin-bottom: -2px;
    cursor: pointer;
    font-size: 1rem;
    font-weight: 600;
    color: color-mix(in oklab, var(--color-neutral-700), transparent 20%);
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .dc-tab:hover {
    color: var(--brand-DC);
  }

  .dc-tab[aria-selected="true"] {
    color: var(--color-neutral-900);
    border-bottom-color: var(--brand-DC);
    font-weight: 700;
  }

  .dc-tab [data-icon] {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .dc-tab__badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1.4rem;
    height: 1.4rem;
    padding: 0 0.3rem;
    border-radius: 999px;
    background: color-mix(in oklab, var(--brand-DC), white 75%);
    color: color-mix(in oklab, var(--brand-DC), black 15%);
    font-size: 0.75rem;
    font-weight: 700;
    line-height: 1;
  }

  .dc-tab[aria-selected="true"] .dc-tab__badge {
    background: var(--brand-DC);
    color: #fff;
  }

  /* ── Step List ────────────────────────────────────────────────────────────── */
  .dc-step-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .dc-step-item {
    display: grid;
    grid-template-columns: 2.5rem minmax(0, 1fr) auto;
    gap: var(--space-s);
    align-items: center;
    padding: var(--space-s) 0;
    border-bottom: 1px dashed color-mix(in oklab, var(--brand-DC), white 65%);
  }

  .dc-step-item:last-child {
    border-bottom: none;
  }

  .dc-step-num {
    font-size: var(--font-size-1);
    font-weight: 800;
    font-variant-numeric: tabular-nums;
    color: color-mix(in oklab, var(--brand-DC), black 15%);
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .dc-step-title {
    margin: 0;
    font-size: var(--font-size-0);
    line-height: 1.4;
    color: var(--foreground-color);
  }

  /* ── Video Preview Card ───────────────────────────────────────────────────── */
  .dc-video-preview {
    display: grid;
    grid-template-columns: minmax(140px, 350px) 1fr;
    gap: var(--space-m);
    background: color-mix(in oklab, var(--brand-DC), white 93%);
    border: 1.5px solid color-mix(in oklab, var(--brand-DC), white 72%);
    border-radius: var(--radius-m);
    padding: var(--space-m);
    align-items: center;
    overflow: hidden;
  }

  .dc-video-preview__thumb {
    border-radius: var(--radius-s);
    overflow: hidden;
    background: #111;
    flex-shrink: 0;
  }

  .dc-video-preview__thumb video {
    width: 100%;
    display: block;
    aspect-ratio: 16 / 9;
    object-fit: cover;
  }

  .dc-video-preview__info {
    display: flex;
    flex-direction: column;
    gap: var(--space-s);
  }

  .dc-video-preview__title {
    margin: 0;
    font-size: var(--font-size-1);
    font-weight: 700;
    line-height: 1.2;
    color: var(--color-neutral-900);
  }

  .dc-video-preview__desc {
    margin: 0;
    font-size: var(--font-size-0);
    line-height: 1.5;
    color: var(--foreground-color);
  }

  .dc-video-preview__actions {
    display: flex;
    gap: var(--space-s);
    flex-wrap: wrap;
  }

  .dc-divider {
    margin: var(--space-xl) 0 var(--space-m);
    display: flex;
    align-items: center;
    gap: var(--space-s);
    font-size: var(--font-size--1);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: color-mix(in oklab, var(--brand-DC), black 20%);
  }

  .dc-divider::before,
  .dc-divider::after {
    content: '';
    flex: 1;
    border-top: 1px solid var(--brand-DC);
  }

  .dc-divider--center {
    justify-content: center;
    color: var(--foreground-color);
  }

  .dc-divider--center::before,
  .dc-divider--center::after {
    flex: 0.6;
  }

  .dc-card {
    border: 2px solid color-mix(in oklab, var(--brand-DC), white 30%);
    border-radius: var(--radius-m);
    padding: var(--space-m);
    background: var(--background-color);
  }

  .dc-card h4 {
    margin-bottom: var(--space-s);
  }

  .dc-material-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-2xs);
  }

  .dc-material-item {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-s);
    align-items: center;
    padding: var(--space-xs) var(--space-s);
    border: 1px solid color-mix(in oklab, var(--brand-DC), white 45%);
    border-radius: var(--radius-s);
    background-color: var(--background-color);
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .dc-material-item:hover {
    border-color: color-mix(in oklab, var(--brand-DC), black 6%);
    transform: translateY(-1px);
  }

  .dc-material-text {
    margin: 0;
  }

  .dc-item-download {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    border: 1px solid color-mix(in oklab,var(--brand-DC),white 45%);
    color: color-mix(in oklab, var(--brand-DC), black 10%);
    background: #fff;
    text-decoration: none;
    border-radius: 10px;
    padding: 0.35rem 0.8rem;
    font-weight: 700;
    font-size: var(--font-size--1);
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .dc-item-download:hover {
    background-color: var(--brand-DC);
    color: #fff;
    border-color: color-mix(in oklab, var(--brand-DC), black 6%);
    transform: translateY(-1px);
  }

  .dc-module-card {
    border: 2px solid color-mix(in oklab, var(--module-card-color, var(--brand-DC)), white 30%);
    border-radius: var(--radius-l);
    padding: var(--space-m);
    background: color-mix(in oklab, var(--module-card-color, var(--brand-DC)), white 90%);
    box-shadow: var(--elevation-1);
    display: flex;
    flex-direction: column;
    gap: var(--space-s);
    align-content: start;
  }

  .dc-module-card h4 {
    color: color-mix(in oklab, var(--module-card-color, var(--brand-DC)), black 2%);
  }

  .dc-card-text {
    color: var(--foreground-color);
  }

  .dc-card-button {
    width: 100%;
    justify-content: center;
    border-radius: 10px;
    margin-top: auto;
    background: var(--module-card-color, var(--brand-DC));
    color: #fff;
    border: 2px solid color-mix(in oklab,var(--module-card-color, var(--brand-DC)),white 30%);
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .dc-card-button:hover {
    background: color-mix(in oklab, var(--module-card-color, var(--brand-DC)), black 10%);
    color: #fff;
    border: 2px solid color-mix(in oklab, var(--module-card-color, var(--brand-DC)), black 10%);
    transform: translateY(-1px);
  }

  .dc-video-card {
    border: 2px solid color-mix(in oklab, var(--brand-DC), white 30%);
    border-radius: var(--radius-m);
    padding: var(--space-m);
    background: var(--background-color);
    box-shadow: var(--elevation-2);
    display: grid;
    gap: var(--space-s);
  }

  .dc-video-card__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-s);
  }

  .dc-video-card__header > section:first-child {
    min-width: 0;
  }

  .dc-video-card__downloads {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    flex-wrap: nowrap;
    flex-shrink: 0;
    justify-content: flex-end;
  }

  .dc-video-download {
    flex-shrink: 0;
    justify-content: center;
    min-width: 150px;
  }

  .dc-video video {
    width: 100%;
    border-radius: var(--radius-m);
    box-shadow: var(--elevation-1);
    background: #000;
  }

  .dc-video__details {
    display: grid;
    gap: var(--space-xs);
  }

  @media (max-width: 900px) {
    .dc-grid--three {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .dc-panel--aim {
      grid-column: 1 / -1;
    }

    .dc-module__download-button {
      width: 100%;
    }

    .dc-module-list {
      --dc-part-column: 96px;
    }
  }

  @media (max-width: 700px) {
    .dc-grid--three {
      grid-template-columns: 1fr;
    }

    .dc-panel--aim {
      grid-column: auto;
    }

    .dc-module-row {
      grid-template-columns: 1fr;
      gap: var(--space-xs);
    }

    .dc-module-row::after {
      display: none;
    }

    .dc-part-button {
      width: fit-content;
      min-width: 7rem;
      justify-self: start;
    }

    .dc-material-item {
      grid-template-columns: 1fr;
      align-items: start;
    }

    .dc-video-card__header {
      flex-direction: column;
    }

    .dc-video-card__downloads {
      width: 100%;
      justify-content: flex-start;
    }

    .dc-video-download {
      width: 100%;
    }

    .dc-package-banner {
      flex-direction: column;
      align-items: stretch;
      padding: var(--space-s) var(--space-m);
    }

    .dc-package-banner__title {
      font-size: var(--font-size-0);
    }

    .dc-package-btn-wrap {
      align-self: stretch;
    }

    .dc-package-btn {
      width: auto;
      justify-content: center;
      white-space: normal;
      text-align: center;
      font-size: 0.9rem;
      display: flex;
      gap: var(--space-s);
      flex-wrap: wrap;
    }

    .dc-video-preview__actions {
      flex-direction: column;
    }

    .dc-video-action-wrap {
      width: 100%;   
    }

    .dc-video-action-wrap .dc-item-download {
      display: flex;
      gap: var(--space-s);
      flex-wrap: wrap;
      justify-content: center;
    }

    .dc-tab {
      padding: 0.5rem 0.7rem;
      font-size: 0.875rem;
      white-space: nowrap;
      gap: 0.3rem;
    }

    .dc-tab [data-icon] {
      display: none;
    }

    .dc-step-item {
      grid-template-columns: 2rem minmax(0, 1fr) auto;
    }

    .dc-video-preview {
      grid-template-columns: 1fr;
    }

    .dc-video-preview__title {
      font-size: var(--font-size-0);
    }
  }

  @media (max-width: 480px) {
    .dc-tab {
      padding: 0.45rem 0.55rem;
      font-size: 0.70rem;
    }

    .dc-tab__badge {
      min-width: 1.2rem;
      height: 1.2rem;
      font-size: 0.7rem;
    }

    .dc-step-item {
      grid-template-columns: 2rem 1fr;
      row-gap: var(--space-xs);
    }

    .dc-step-item > :last-child {
      grid-column: 2;
      justify-self: start;
    }

    .dc-step-title {
      font-size: 0.9rem;
    }

  }

  /* ── Dark mode overrides ─────────────────────────────────────────────────── */
  :global(html.darkmode) .dc-panel {
    background: linear-gradient(180deg, var(--dm-surface-2) 0%, var(--dm-surface-1) 100%);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-panel__content {
    color: var(--dm-fg-muted);
    border-top-color: var(--dm-border);
  }

  :global(html.darkmode) .dc-list li {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-list li + li {
    border-top-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-aim-text {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-section__subtitle {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-goal {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-meta-item {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-module-row:not(:last-child)::after {
    border-left-color: color-mix(in oklab, var(--brand-DC) 40%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-module {
    background: linear-gradient(155deg, var(--dm-surface-2) 0%, var(--dm-surface-1) 55%);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-module__header h3 a {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-module__header h3 a:hover,
  :global(html.darkmode) .dc-module__header h3 a:focus-visible {
    color: var(--brand-DC);
  }

  :global(html.darkmode) .dc-module__toggle {
    background: color-mix(in oklab, var(--brand-DC) 18%, var(--dm-surface-3));
    border-color: color-mix(in oklab, var(--brand-DC) 35%, var(--dm-border));
    color: var(--brand-DC);
  }

  :global(html.darkmode) .dc-module__header:hover .dc-module__toggle {
    background: var(--brand-DC);
    border-color: var(--brand-DC);
    color: #1c1917;
  }

  :global(html.darkmode) .dc-module__download {
    background: rgba(255, 255, 255, 0.04);
    border-color: var(--brand-DC);
  }

  :global(html.darkmode) .dc-module__download-button:hover {
    background: var(--dm-surface-3);
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-divider {
    color: color-mix(in oklab, var(--brand-DC), white 20%);
  }

  :global(html.darkmode) .dc-divider::before,
  :global(html.darkmode) .dc-divider::after {
    border-top-color: color-mix(in oklab, var(--brand-DC) 50%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-divider--center {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-card {
    background: var(--dm-surface-2);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-material-item {
    background: linear-gradient(90deg, rgba(255,255,255,0.06) 0%, rgba(255,255,255,0.02) 100%);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-material-item:hover {
    border-color: var(--brand-DC);
  }

  :global(html.darkmode) .dc-item-download {
    background: var(--dm-surface-1);
    color: color-mix(in oklab, var(--brand-DC), white 25%);
    border-color: color-mix(in oklab, var(--brand-DC) 40%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-item-download:hover {
    background: var(--brand-DC);
    color: #1c1917;
    border-color: var(--brand-DC);
  }

  :global(html.darkmode) .dc-video-card {
    background: var(--dm-surface-2);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-module-card {
    background: color-mix(in oklab, var(--module-card-color, var(--brand-DC)) 12%, var(--dm-surface-1));
    border-color: color-mix(in oklab, var(--module-card-color, var(--brand-DC)) 40%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-module-card h4 {
    color: color-mix(in oklab, var(--module-card-color, var(--brand-DC)), white 35%);
  }

  :global(html.darkmode) .dc-card-text {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-card-button {
    background: var(--module-card-color, var(--brand-DC));
    border-color: color-mix(in oklab, var(--module-card-color, var(--brand-DC)), black 10%);
    color: #ffffff;
  }

  :global(html.darkmode) .dc-card-button:hover {
    background: color-mix(in oklab, var(--module-card-color, var(--brand-DC)), white 15%);
    border-color: var(--module-card-color, var(--brand-DC));
    color: #ffffff;
  }

  :global(html.darkmode) .dc-teaching-guide {
    background: color-mix(in oklab, var(--brand-DC) 14%, var(--dm-surface-2));
    border-color: color-mix(in oklab, var(--brand-DC) 35%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-package-banner {
    background: color-mix(in oklab, var(--brand-DC) 14%, var(--dm-surface-2));
    border-color: color-mix(in oklab, var(--brand-DC) 35%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-package-banner__title {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-package-banner__sub {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-tabs__bar {
    border-bottom-color: color-mix(in oklab, var(--brand-DC) 30%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-tab {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-tab:hover {
    color: color-mix(in oklab, var(--brand-DC), white 20%);
  }

  :global(html.darkmode) .dc-tab[aria-selected="true"] {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-tab__badge {
    background: color-mix(in oklab, var(--brand-DC) 25%, var(--dm-surface-3));
    color: color-mix(in oklab, var(--brand-DC), white 30%);
  }

  :global(html.darkmode) .dc-tab[aria-selected="true"] .dc-tab__badge {
    background: var(--brand-DC);
    color: #1c1917;
  }

  :global(html.darkmode) .dc-step-num {
    color: color-mix(in oklab, var(--brand-DC), white 25%);
  }

  :global(html.darkmode) .dc-step-title {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-step-item {
    border-bottom-color: color-mix(in oklab, var(--brand-DC) 20%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-video-preview {
    background: color-mix(in oklab, var(--brand-DC) 10%, var(--dm-surface-2));
    border-color: color-mix(in oklab, var(--brand-DC) 28%, var(--dm-border-sub));
  }

  :global(html.darkmode) .dc-video-preview__title {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-video-preview__desc {
    color: var(--dm-fg-muted);
  }

  /* ── Vertical tabs ──────────────────────────────────────────────────────── */
  .dc-tabs--vertical {
    display: grid;
    grid-template-columns: max-content 1fr;
    align-items: start;
    gap: 0;
  }

  .dc-tabs--vertical .dc-tabs__bar {
    flex-direction: column;
    border-bottom: none;
    border-right: 2px solid color-mix(in oklab, var(--brand-DC), white 70%);
    overflow-x: unset;
    padding-right: 0;
    margin-bottom: 0;
    gap: 10px;
  }

  .dc-tabs--vertical .dc-tab {
    justify-content: flex-start;
    white-space: nowrap;
    border-bottom-color: transparent;
    border-right: 3px solid transparent;
    margin-bottom: 0;
    margin-right: -2px;
    padding-right: var(--space-l);
    font-size: var(--font-size--3);
    font-weight: 800;
  }

  .dc-tabs--vertical .dc-tab[aria-selected="true"] {
    border-bottom-color: transparent;
    border-right-color: var(--brand-DC);
  }

  .dc-tabs-v__body {
    padding-left: var(--space-l);
  }

  @media (max-width: 640px) {
    .dc-tabs--vertical {
      grid-template-columns: 1fr;
    }

    .dc-tabs--vertical .dc-tabs__bar {
      flex-direction: row;
      border-right: none;
      border-bottom: 2px solid color-mix(in oklab, var(--brand-DC), white 70%);
      overflow-x: auto;
      padding-right: var(--space-2xs);
      margin-bottom: var(--space-m);
    }

    .dc-tabs--vertical .dc-tab {
      white-space: nowrap;
      border-right-color: transparent;
      border-bottom: 3px solid transparent;
      margin-right: 0;
      margin-bottom: -2px;
      padding-right: 1rem;
    }

    .dc-tabs--vertical .dc-tab[aria-selected="true"] {
      border-right-color: transparent;
      border-bottom-color: var(--brand-DC);
    }

    .dc-tabs-v__body {
      padding-left: 0;
    }
  }

  :global(html.darkmode) .dc-tabs--vertical .dc-tabs__bar {
    border-right-color: color-mix(in oklab, var(--brand-DC) 30%, var(--dm-border-sub));
  }

  /* ── AOO tab icon + aim panel ───────────────────────────────────────────── */
  .dc-tab-img-icon {
    width: 22px;
    height: 22px;
    object-fit: contain;
    flex-shrink: 0;
    border-radius: 50%;
  }

  .dc-step-item--plain {
    grid-template-columns: 2.5rem minmax(0, 1fr);
  }

  .dc-aim-content {
    display: flex;
    align-items: center;
    gap: var(--space-l);
    padding: var(--space-s) 0;
  }

  /* ── Related Modules compact list ───────────────────────────────────────── */
  .dc-rm-list {
    border: 2px solid color-mix(in oklab, var(--color-neutral-500), white 55%);
    border-radius: var(--radius-l);
    overflow: hidden;
  }

  .dc-rm-row {
    display: grid;
    grid-template-columns: 56px 1fr auto auto;
    gap: var(--space-m);
    align-items: center;
    padding: var(--space-m) var(--space-l);
    text-decoration: none;
    border-left: 3px solid transparent;
    transition: background 0.15s ease, border-color 0.15s ease;
  }

  .dc-rm-row + .dc-rm-row {
    border-top: 1px solid color-mix(in oklab, var(--color-neutral-500), white 55%);
  }

  .dc-rm-row:hover {
    background: color-mix(in oklab, var(--rm-color, var(--brand-DC)), white 93%);
    border-left-color: var(--rm-color, var(--brand-DC));
  }

  .dc-rm-icon {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    overflow: hidden;
    flex-shrink: 0;
  }

  .dc-rm-icon img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .dc-rm-body {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    min-width: 0;
  }

  .dc-rm-title {
    font-size: var(--font-size-0);
    font-weight: 700;
    color: var(--color-neutral-900);
    line-height: 1.2;
  }

  .dc-rm-desc {
    font-size: var(--font-size--1);
    color: var(--foreground-color);
    line-height: 1.4;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .dc-rm-meta {
    display: flex;
    gap: var(--space-s);
    align-items: center;
    flex-shrink: 0;
  }

  .dc-rm-stat {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-size: var(--font-size--1);
    font-weight: 600;
    color: var(--foreground-color);
    white-space: nowrap;
  }

  .dc-rm-stat svg {
    width: 1.1rem;
    height: 1.1rem;
    flex-shrink: 0;
    opacity: 0.6;
  }

  .dc-rm-row:hover .dc-rm-stat svg {
    color: var(--rm-color);
    opacity: 1;
    transition: color 0.2s ease, opacity 0.2s ease;
  }

  .dc-rm-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--color-neutral-400);
    transition: color 0.2s ease, transform 0.2s ease;
  }

  .dc-rm-arrow svg {
    width: 1.1rem;
    height: 1.1rem;
  }

  .dc-rm-row:hover .dc-rm-arrow {
    color: var(--rm-color, var(--brand-DC));
    transform: translateX(3px);
  }

  @media (max-width: 640px) {
    .dc-rm-row {
      grid-template-columns: 44px 1fr auto;
      gap: var(--space-s);
      padding: var(--space-s) var(--space-m);
    }

    .dc-rm-icon {
      width: 44px;
      height: 44px;
    }

    .dc-rm-meta {
      display: none;
    }
  }

  :global(html.darkmode) .dc-rm-list {
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-rm-row + .dc-rm-row {
    border-top-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .dc-rm-row:hover {
    background: color-mix(in oklab, var(--rm-color, var(--brand-DC)) 12%, var(--dm-surface-2));
  }

  :global(html.darkmode) .dc-rm-title {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .dc-rm-desc {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .dc-rm-stat {
    color: var(--dm-fg-muted);
  }

</style>
