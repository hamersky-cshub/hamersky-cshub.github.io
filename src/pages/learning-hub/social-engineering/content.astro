---
import DefaultLayout from '@layouts/DefaultLayout.astro'
import PageHeader from '@components/PageHeader.astro'
import { Icon } from 'astro-icon/components'
import { localizePath } from 'src/i18n/i18n'
import { getModule } from 'src/data/modules'
import type { ModuleId } from 'src/data/modules'
import { modulePartsData, mergeParts } from 'src/data/moduleParts'
import type { TranslationPart, TeachersGuide, RelatedModuleCard, MergedPart, MergedMaterial, MergedActivityStep } from 'src/data/moduleParts'
import { useTranslations } from 'src/i18n/index'

const locale = Astro.currentLocale
const { t, v } = useTranslations(locale)

const objectives = v<ReadonlyArray<string>>('contentSE.objectives') ?? []
const outcomes = v<ReadonlyArray<string>>('contentSE.outcomes') ?? []
const teachersGuide = v<TeachersGuide>('contentSE.teachersGuide') ?? {}
const currentModule = getModule('se')!
const parts = mergeParts('se', v<ReadonlyArray<TranslationPart>>('contentSE.parts') ?? [])
const relatedModuleCards = (v<ReadonlyArray<RelatedModuleCard>>('contentSE.relatedModuleCards') ?? []).map(card => { const mod = getModule(card.moduleId); if (!mod) return card; return { ...card, parts: modulePartsData[mod.id].length, materials: mod.materials, videos: mod.videos, challenges: mod.challenges, games: mod.games } })

const hasCount = (value: unknown): value is number => typeof value === 'number' && value > 0

const formatCount = (count: number, singularKey: string, pluralKey: string) =>
  `${count} ${t(count === 1 ? singularKey : pluralKey)}`

const getPartTitle = (part: MergedPart) => (part.titleKey ? t(part.titleKey) : String(part.title ?? ''))

const moduleImages = {
  dc: '/images/learning-hub/01_digital-citizenship.png',
  ap: '/images/learning-hub/02_attacker-perspective.png',
  at: '/images/learning-hub/03_authentication.png',
  dp: '/images/learning-hub/04_data-privacy.png',
  se: '/images/learning-hub/05_social-engineering.png',
  mw: '/images/learning-hub/06_malware.png',
  da: '/images/learning-hub/07_digital-abuse.png',
}
---

<DefaultLayout title={t('modules.se.title')}>
  <PageHeader
      title={t('modules.se.title')}
      subtitle={t('contentSE.subtitle')}
      underlineColor="var(--brand-SE)"
      bgType="module"
      buttons={[
        { 
          text: t('contentPage.downloadGuide'), 
          href: currentModule.teachersGuide.href,
          variant: "primary",
          icon: "lucide:download",
          download: teachersGuide.download ?? '',
        },
        { 
          text: t('contentPage.downloadAll'), 
          href: "/materials/social-engineering/social-engineering.zip", 
          variant: "primary", 
          icon: "lucide:download", 
          download: "Social Engineering Material Package" 
        },
      ]}
      />

  <!-- Main Content Area -->
  <section class="my-12">
    <div class="container">

      <!-- Learning Overview: Aim / Objectives / Outcomes tabs -->
      <div class="aoo-card">
        <div class="se-tabs">
          <div class="se-tabs__bar" role="tablist">
            <button class="se-tab" role="tab" aria-selected="true" data-panel="aoo-aim">
              <img class="se-tab-img-icon" src="/images/learning-hub/aim.png" alt="" aria-hidden="true" />
              {t('contentPage.aimTitle')}
            </button>
            <button class="se-tab" role="tab" aria-selected="false" data-panel="aoo-objectives">
              <img class="se-tab-img-icon" src="/images/learning-hub/objectives.png" alt="" aria-hidden="true" />
              {t('contentPage.objectivesTitle')}
              <span class="se-tab__badge">{objectives.length}</span>
            </button>
            <button class="se-tab" role="tab" aria-selected="false" data-panel="aoo-outcomes">
              <img class="se-tab-img-icon" src="/images/learning-hub/outcomes.png" alt="" aria-hidden="true" />
              {t('contentPage.outcomesTitle')}
              <span class="se-tab__badge">{outcomes.length}</span>
            </button>
          </div>

          <div class="se-tab-panel" role="tabpanel" id="aoo-aim">
            <div class="se-aim-content">
              <p class="se-aim-text">{t('contentSE.aim')}</p>
            </div>
          </div>

          <div class="se-tab-panel" role="tabpanel" id="aoo-objectives" hidden>
            <ul class="se-step-list">
              {objectives.map((obj, i) => (
                <li class="se-step-item se-step-item--plain">
                  <span class="se-step-num">{String(i + 1).padStart(2, '0')}</span>
                  <p class="se-step-title">{obj}</p>
                </li>
              ))}
            </ul>
          </div>

          <div class="se-tab-panel" role="tabpanel" id="aoo-outcomes" hidden>
            <ul class="se-step-list">
              {outcomes.map((outcome, i) => (
                <li class="se-step-item se-step-item--plain">
                  <span class="se-step-num">{String(i + 1).padStart(2, '0')}</span>
                  <p class="se-step-title">{outcome}</p>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>

      <!-- Section Divider: Module Content -->
      <div class="se-divider se-divider--center" style="margin-top: var(--space-xl);" id="module-content"><b>{t('contentPage.moduleContent')}</b></div>

      <!-- Module Parts List -->
      <div class="se-section">
        <div class="se-module-list">
          <div class="se-module-row">
            <a class="se-part-button" href="#teachers-guide">{t('contentPage.guideLabel')}</a>
            <div class="se-teaching-guide" id="teachers-guide">
              <div class="se-package-banner__copy">
                <h3>{t('contentPage.teachersGuideTitle')}</h3>
                <p class="se-package-banner__sub">{t('contentPage.guideDescription')}</p>
              </div>
              <div class="se-package-btn-wrap">
                <a
                  class="button has-icon se-package-btn"
                  href={currentModule.teachersGuide.href}
                  download={teachersGuide.download ?? ''}
                  aria-label={String(teachersGuide.ariaLabel ?? '')}
                >
                  <Icon name="lucide:download" />
                  {t('contentPage.teachersGuideDownload')}
                </a>
              </div>
            </div>
          </div>

          {parts.map((part) => {
            const meta = part.meta ?? {}
            const included = part.included ?? {}
            const partMaterials = included.materials ?? []
            const partActivity = included.activityPlan ?? []
            const featuredVideo = part.featuredVideo
            const title = getPartTitle(part)
            const firstPanel = partActivity.length ? 'activity' : partMaterials.length ? 'materials' : 'video'

            return (
              <div class="se-module-row">
                <a class="se-part-button" href={`#${part.anchorId}`}>{t('hub.partLabel')} {part.number}</a>
                <details class="se-module" id={part.anchorId}>
                  <summary class="se-module__header">
                    <div class="se-module__copy">
                      <h3>{title}</h3>
                      <div class="se-module__meta">
                        {hasCount(meta.steps) ? (
                          <span class="se-meta-item"><Icon name="lucide:list-checks" /> {formatCount(meta.steps, 'contentPage.stepSingular', 'contentPage.stepPlural')}</span>
                        ) : null}
                        {hasCount(meta.materials) ? (
                          <span class="se-meta-item"><Icon name="lucide:file-text" /> {formatCount(meta.materials, 'contentPage.materialSingular', 'contentPage.materialPlural')}</span>
                        ) : null}
                        {hasCount(meta.videos) ? (
                          <span class="se-meta-item"><Icon name="lucide:play-circle" /> {formatCount(meta.videos, 'contentPage.videoSingular', 'contentPage.videoPlural')}</span>
                        ) : null}
                      </div>
                      <p class="se-goal"><b>{t('contentPage.goalLabel')}</b> {String(part.goal ?? '')}</p>
                    </div>
                    <span class="se-module__toggle" aria-hidden="true">
                      <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
                        <path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                      </svg>
                    </span>
                  </summary>
                  <div class="se-module__content">
                    <div class="se-package-banner">
                      <div class="se-package-banner__copy">
                        <p class="se-package-banner__title">{t('contentPage.materialPackage')}</p>
                        <p class="se-package-banner__sub">{t('contentPage.materialPackageSub')}</p>
                      </div>
                      <div class="se-package-btn-wrap">
                        <a
                          class="button has-icon se-package-btn"
                          href={String(part.bundle?.href ?? '')}
                          download={String(part.bundle?.filename ?? '')}
                        >
                          <Icon name="lucide:download" />
                          {t('contentPage.downloadPart')}
                        </a>
                      </div>
                    </div>

                    {(partMaterials.length || partActivity.length || featuredVideo) ? (
                      <div class="se-tabs">
                        <div class="se-tabs__bar" role="tablist">
                          {partActivity.length ? (
                            <button
                              class="se-tab"
                              role="tab"
                              aria-selected={firstPanel === 'activity' ? 'true' : 'false'}
                              data-panel={`activity-${part.anchorId}`}
                            >
                              <Icon name="lucide:list" />
                              {t('contentPage.activityPlan')}
                              <span class="se-tab__badge">{partActivity.length}</span>
                            </button>
                          ) : null}
                          {partMaterials.length ? (
                            <button
                              class="se-tab"
                              role="tab"
                              aria-selected={firstPanel === 'materials' ? 'true' : 'false'}
                              data-panel={`materials-${part.anchorId}`}
                            >
                              <Icon name="lucide:file-text" />
                              {t('contentPage.materialPlural')}
                              <span class="se-tab__badge">{partMaterials.length}</span>
                            </button>
                          ) : null}
                          {featuredVideo ? (
                            <button
                              class="se-tab"
                              role="tab"
                              aria-selected={firstPanel === 'video' ? 'true' : 'false'}
                              data-panel={`video-${part.anchorId}`}
                            >
                              <Icon name="lucide:video" />
                              {t('contentPage.videoSingular')}
                              <span class="se-tab__badge">1</span>
                            </button>
                          ) : null}
                        </div>

                        {partMaterials.length ? (
                          <div
                            class="se-tab-panel"
                            role="tabpanel"
                            id={`materials-${part.anchorId}`}
                            hidden={firstPanel !== 'materials'}
                          >
                            <ul class="se-step-list">
                              {partMaterials.map((item: MergedMaterial, i: number) => (
                                <li class="se-step-item">
                                  <span class="se-step-num">{String(i + 1).padStart(2, '0')}</span>
                                  <p class="se-step-title"><b>{String(item.kind ?? '')}:</b> {String(item.name ?? '')}</p>
                                  <a
                                    class="se-item-download"
                                    href={String(item.href ?? '')}
                                    download={String(item.filename ?? '')}
                                    aria-label={String(item.ariaLabel ?? '')}
                                  >
                                    <Icon name="lucide:download" />
                                    <span>{t('contentPage.download')}</span>
                                  </a>
                                </li>
                              ))}
                            </ul>
                          </div>
                        ) : null}

                        {partActivity.length ? (
                          <div
                            class="se-tab-panel"
                            role="tabpanel"
                            id={`activity-${part.anchorId}`}
                            hidden={firstPanel !== 'activity'}
                          >
                            <ul class="se-step-list">
                              {partActivity.map((step: MergedActivityStep) => (
                                <li class="se-step-item se-step-item--plain">
                                  <span class="se-step-num">{String(step.stepNumber ?? 0).padStart(2, '0')}</span>
                                  <p class="se-step-title"><b>{t('contentPage.stepSingular')} {String(step.stepNumber ?? '')}:</b> {String(step.title ?? '')}</p>
                                </li>
                              ))}
                            </ul>
                          </div>
                        ) : null}

                        {featuredVideo ? (
                          <div
                            class="se-tab-panel"
                            role="tabpanel"
                            id={`video-${part.anchorId}`}
                            hidden={firstPanel !== 'video'}
                          >
                            <div class="se-video-preview">
                              <div class="se-video-preview__thumb">
                                <video
                                  controls
                                  preload="metadata"
                                  poster={String(featuredVideo.posterSrc ?? '')}
                                >
                                  <source src={String(featuredVideo.videoSrc ?? '')} />
                                  {(featuredVideo.tracks ?? []).map((track) => (
                                    <track
                                      label={String(track.label ?? '')}
                                      kind="subtitles"
                                      srclang={String(track.srclang ?? '')}
                                      src={String(track.src ?? '')}
                                    />
                                  ))}
                                </video>
                              </div>
                              <div class="se-video-preview__info">
                                <h4 class="se-video-preview__title">{String(featuredVideo.title ?? '')}</h4>
                                <p class="se-video-preview__desc">{String(featuredVideo.supportText ?? '')}</p>
                                <div class="se-video-preview__actions">
                                  <div class="se-video-action-wrap">
                                    <a
                                      class="se-item-download"
                                      href={String(featuredVideo.downloads?.video?.href ?? '')}
                                      download={String(featuredVideo.downloads?.video?.filename ?? '')}
                                      aria-label={String(featuredVideo.downloads?.video?.ariaLabel ?? '')}
                                    >
                                      <Icon name="lucide:download" />
                                      <span>{t('contentPage.videoSingular')}</span>
                                    </a>
                                  </div>
                                  <div class="se-video-action-wrap">
                                    <a
                                      class="se-item-download"
                                      href={String(featuredVideo.downloads?.subtitles?.href ?? '')}
                                      download={String(featuredVideo.downloads?.subtitles?.filename ?? '')}
                                      aria-label={String(featuredVideo.downloads?.subtitles?.ariaLabel ?? '')}
                                    >
                                      <Icon name="lucide:captions" />
                                      <span>{t('contentPage.subtitlesSingular')}</span>
                                    </a>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </div>
                        ) : null}
                      </div>
                    ) : null}
                  </div>
                </details>
              </div>
            )
          })}
        </div>
      </div>

      <!-- Section Divider: Related Modules -->
      <div class="se-divider se-divider--center"><b>{t('contentPage.relatedModules')}</b></div>

      <!-- Related Modules -->
      <div class="se-section">
        <div class="se-section__header">
          <h2>{t('contentPage.relatedModules')}</h2>
          <p class="se-section__subtitle">{t('contentPage.relatedModulesSubtitle')}</p>
        </div>

        <div class="se-rm-list">
          {relatedModuleCards.map((card, idx) => {
            const moduleId = String(card.moduleId ?? '')
            const title = t(`modules.${moduleId}.title`)
            const href = localizePath(locale, String(card.href ?? ''))
            const image = moduleImages[moduleId as keyof typeof moduleImages]
            const brandVar = `var(--brand-${String(card.brand ?? '')})`

            return (
              <a
                class={`se-rm-row${idx === 0 ? ' se-rm-row--first' : ''}`}
                href={href}
                style={`--rm-color: ${brandVar};`}
              >
                <div class="se-rm-icon">
                  <img src={image} alt="" decoding="async" loading="eager" />
                </div>
                <div class="se-rm-body">
                  <strong class="se-rm-title">{title}</strong>
                  <span class="se-rm-desc">{String(card.description ?? '')}</span>
                </div>
                <div class="se-rm-meta" aria-label={title}>
                  {hasCount(card.parts) ? (
                    <span class="se-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
                      {card.parts}
                    </span>
                  ) : null}
                  {hasCount(card.materials) ? (
                    <span class="se-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="8" y1="13" x2="16" y2="13"/><line x1="8" y1="17" x2="16" y2="17"/></svg>
                      {card.materials}
                    </span>
                  ) : null}
                  {hasCount(card.videos) ? (
                    <span class="se-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/></svg>
                      {card.videos}
                    </span>
                  ) : null}
                  {hasCount(card.challenges) ? (
                    <span class="se-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
                      {card.challenges}
                    </span>
                  ) : null}
                  {hasCount(card.games) ? (
                    <span class="se-rm-stat">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 12h4M8 10v4"/><circle cx="15" cy="11" r="0.8" fill="currentColor" stroke="none"/><circle cx="17" cy="13" r="0.8" fill="currentColor" stroke="none"/><path d="M2 9a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v1c0 1-0.5 2-1 2.5L19 17a3 3 0 0 1-2.5 1.5h-9A3 3 0 0 1 5 17l-2-4.5C2.5 12 2 11 2 10V9z"/></svg>
                      {card.games}
                    </span>
                  ) : null}
                </div>
                <span class="se-rm-arrow" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none">
                    <path d="M8 5l8 7-8 7" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </span>
              </a>
            )
          })}
        </div>
      </div>
    </div>
  </section>
</DefaultLayout>

<script>
  const expandFromHash = () => {
    const hash = window.location.hash.slice(1)
    if (!hash) return

    const target = document.getElementById(hash)
    if (target instanceof HTMLDetailsElement) {
      target.open = true
      requestAnimationFrame(() => {
        target.scrollIntoView({ behavior: 'smooth', block: 'start' })
      })
    }
  }

  document.addEventListener('astro:page-load', expandFromHash)
  expandFromHash()

  const initTabs = () => {
    document.querySelectorAll('.se-tabs').forEach(tabs => {
      const bar = tabs.querySelector('.se-tabs__bar')
      if (!bar) return
      bar.addEventListener('click', e => {
        const btn = (e.target as Element).closest('[role="tab"]') as HTMLElement | null
        if (!btn) return
        const targetId = btn.dataset.panel
        if (!targetId) return
        bar.querySelectorAll('[role="tab"]').forEach(t => {
          t.setAttribute('aria-selected', t === btn ? 'true' : 'false')
        })
        tabs.querySelectorAll('[role="tabpanel"]').forEach(p => {
          (p as HTMLElement).hidden = p.id !== targetId
        })
      })
    })
  }

  document.addEventListener('astro:page-load', initTabs)
  initTabs()
</script>

<style>
  .se-grid {
    display: grid;
    gap: var(--space-l);
  }

  .se-grid--two {
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  }

  .se-grid--three {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  :global(.se-panel) {
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    border: 2px solid #dbe2ea;
    border-radius: var(--radius-l);
    padding: var(--space-l);
    box-shadow: var(--elevation-1);
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  }

  .se-panel:hover,
  .se-panel:focus-within {
    transform: translateY(-4px);
    box-shadow: var(--elevation-3);
    border: 2px solid color-mix(in oklab,var(--brand-SE),white 60%);
  }

  .se-grid--learning-overview {
    align-items: stretch;
  }

  .se-panel--compact {
    padding: var(--space-m);
    display: flex;
    flex-direction: column;
  }

  .se-panel--learning {
    height: 100%;
  }

  .se-panel--objectives {
    background: linear-gradient(165deg, color-mix(in oklab, var(--color-neutral-100), white 42%), color-mix(in oklab, var(--color-neutral-300), white 28%));
  }

  .se-panel--outcomes {
    background: linear-gradient(165deg, color-mix(in oklab, var(--color-neutral-100), white 40%), color-mix(in oklab, var(--color-neutral-300), white 26%));
  }

  .se-panel--learning h3 {
    margin: 0;
    min-height: 2.6rem;
    display: flex;
    align-items: flex-start;
    font-size: clamp(1.35rem, 1vw + 1rem, 1.9rem);
    line-height: 1.1;
  }

  .se-panel__content {
    margin-top: var(--space-xs);
    border-top: 2px solid color-mix(in oklab, var(--color-neutral-500), white 35%);
    padding-top: var(--space-s);
  }

  .se-list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0;
  }

  .se-list li {
    border: 0;
    border-radius: 0;
    background: transparent;
    padding: 0;
    font-size: var(--font-size--1);
    line-height: 1.5;
  }

  .se-list li + li {
    margin-top: var(--space-s);
    padding-top: var(--space-s);
    border-top: 1px solid color-mix(in oklab, var(--color-neutral-500), white 55%);
  }

  .se-aim-text {
    margin: 0;
    line-height: 1.32;
    font-size: var(--text-xl);
    color: var(--foreground-color);
  }

  .se-section {
    margin-top: var(--space-xl);
  }

  .se-section__header {
    display: grid;
    gap: var(--space-2xs);
    margin-bottom: var(--space-l);
  }

  .se-section__subtitle {
    color: var(--foreground-color);
  }

  .se-module-list {
    --se-part-column: clamp(88px, 11vw, 120px);
    --se-part-gap: var(--space-l);
    --se-part-button-height: 2.8rem;
    display: grid;
    gap: var(--se-part-gap);
  }

  .se-module-row {
    position: relative;
    display: grid;
    grid-template-columns: var(--se-part-column) minmax(0, 1fr);
    gap: var(--space-m);
    align-items: start;
  }

  .se-module-row:not(:last-child)::after {
    content: '';
    position: absolute;
    left: calc(var(--se-part-column) / 2);
    top: calc(var(--se-part-button-height) + var(--space-2xs));
    height: calc(100% + var(--se-part-gap) - var(--se-part-button-height) - var(--space-s));
    border-left: 2px dashed color-mix(in oklab, var(--brand-SE), white 50%);
    transform: translateX(-50%);
    pointer-events: none;
  }

  .se-part-button {
    min-height: var(--se-part-button-height);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    padding: 0 var(--space-s);
    border: 2px solid var(--brand-SE);
    border-radius: 0.5rem;
    background: var(--brand-SE);
    color: #fff;
    box-shadow: var(--elevation-1);
    text-decoration: none;
    font-size: var(--font-size--3);
    font-weight: 800;
    line-height: 1;
    white-space: nowrap;
    position: relative;
    z-index: 1;
    transition: transform 0.2s ease, background-color 0.2s ease, color 0.2s ease;
  }

  .se-part-button:hover {
    background: transparent;
    color: var(--foreground-color);
    transform: translateY(-1px);
  }

  .se-part-button:focus-visible {
    outline: 2px solid color-mix(in oklab, var(--brand-SE), black 20%);
    outline-offset: 2px;
  }

  :global(.se-module) {
    border: 2px solid color-mix(in oklab, var(--brand-SE), white 60%);
    border-radius: 1.25rem;
    padding: 0;
    background: linear-gradient(155deg, color-mix(in oklab, var(--brand-SE), white 92%), #ffffff 55%);
    box-shadow: var(--elevation-2);
    margin-bottom: 0;
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .se-module:hover {
    transform: translateY(-3px);
    box-shadow: var(--elevation-3);
  }

  .se-module__header {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--space-m);
    align-items: center;
    list-style: none;
    cursor: pointer;
    padding: clamp(1rem, 2vw, 1.75rem);
  }

  .se-module__header::-webkit-details-marker {
    display: none;
  }

  .se-module__header::marker {
    content: '';
  }

  .se-module__header h3 {
    margin: 0;
    font-size: clamp(1.6rem, 1.3rem + 1vw, 2.4rem);
    line-height: 1.1;
    font-weight: 900;
    text-decoration: underline;
    text-decoration-color: var(--brand-SE);
    text-underline-offset: 0.35rem;
    text-decoration-thickness: 0.15rem;
  }

  .se-module__header h3 a {
    color: var(--color-neutral-900);
    text-decoration: none;
  }

  .se-module__header h3 a:hover,
  .se-module__header h3 a:focus-visible {
    color: var(--brand-SE);
  }

  .se-module__toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    border-radius: 50%;
    border: 2px solid color-mix(in oklab, var(--brand-SE), white 60%);
    background: color-mix(in oklab, var(--brand-SE), white 86%);
    color: var(--brand-SE);
    pointer-events: none;
    transition: background 0.2s ease, border-color 0.2s ease;
  }

  .se-module__header:hover .se-module__toggle {
    background: var(--brand-SE);
    border-color: var(--brand-SE);
    color: #ffffff;
  }

  .se-module__toggle svg {
    width: 1.1rem;
    height: 1.1rem;
    transition: transform 0.3s ease;
  }

  .se-module:not([open]) .se-module__toggle svg {
    transform: rotate(0deg);
  }

  .se-module[open] .se-module__toggle svg {
    transform: rotate(90deg);
  }

  .se-module__content {
    padding: 0 clamp(1rem, 2vw, 1.75rem) clamp(1rem, 2vw, 1.75rem);
  }

  .se-goal {
    font-size: 1.05rem;
    line-height: 1.5;
    margin-top: var(--space-xs);
    color: var(--foreground-color);
    max-width: 70ch;
  }

  .se-module__meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: var(--space-s);
  }

  .se-meta-item {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--foreground-color);
  }

  .se-meta-item [data-icon] {
    width: 1.1rem;
    height: 1.1rem;
    flex-shrink: 0;
    color: var(--brand-SE);
  }

  /* ── Teaching Guide ──────────────────────────────────────────────────────── */
  .se-teaching-guide {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-xl);
    padding: var(--space-m) var(--space-l);
    border-radius: 1.25rem;
    background: color-mix(in oklab, var(--brand-SE), white 88%);
    border: 1.5px solid color-mix(in oklab, var(--brand-SE), white 60%);
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .se-teaching-guide:hover {
    transform: translateY(-3px);
    box-shadow: var(--elevation-3);
  }

  .se-teaching-guide h3 {
    margin: 0;
    font-size: clamp(1.6rem, 1.3rem + 1vw, 2.4rem);
    line-height: 1.1;
    font-weight: 900;
    text-decoration: underline;
    text-decoration-color: var(--brand-SE);
    text-underline-offset: 0.35rem;
    text-decoration-thickness: 0.15rem;
  }

  /* ── Package Banner ───────────────────────────────────────────────────────── */
  .se-package-banner {
    margin-top: var(--space-m);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-s);
    padding: var(--space-m) var(--space-l);
    border-radius: var(--radius-m);
    background: color-mix(in oklab, var(--brand-SE), white 88%);
    border: 1.5px solid color-mix(in oklab, var(--brand-SE), white 60%);
    overflow: hidden;
  }

  .se-package-banner__copy {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .se-package-banner__title {
    margin: 0;
    font-weight: 700;
    font-size: var(--font-size-1);
    color: var(--color-neutral-900);
  }

  .se-package-banner__sub {
    margin-top: var(--space-xs);
    font-size: var(--font-size-0);
    color: var(--foreground-color);
  }

  .se-package-btn-wrap {
    flex-shrink: 0;
  }

  .se-package-btn {
    border-radius: 10px;
    background: var(--brand-SE);
    color: #fff;
    border: 2px solid color-mix(in oklab, var(--brand-SE), black 10%);
    transition: background-color 0.2s ease, transform 0.2s ease;
  }

  .se-package-btn:hover {
    background: color-mix(in oklab, var(--brand-SE), black 12%);
    color: #fff;
    transform: translateY(-1px);
  }

  /* ── AoO Overview Card ──────────────────────────────────────────────────── */
  .aoo-card {
    display: flex;
    flex-direction: column;
    padding: 1.25rem;
    border: 1px solid #dbe2ea;
    border-radius: 1rem;
    background: linear-gradient(180deg, #f8fafc 0%, #ffffff 100%);
    box-shadow: var(--elevation-1);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    margin-bottom: var(--space-l);
  }

  .aoo-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--elevation-3);
  }

  :global(html.darkmode) .aoo-card {
    background: linear-gradient(180deg, var(--dm-surface-2) 0%, var(--dm-surface-1) 100%);
    border-color: var(--dm-border-sub);
  }

  .aoo-card > .se-tabs {
    margin-top: 0;
  }

  /* ── Tabs ─────────────────────────────────────────────────────────────────── */
  .se-tabs {
    margin-top: var(--space-m);
  }

  .se-tabs__bar {
    display: flex;
    gap: 0;
    flex-wrap: nowrap;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    border-bottom: 2px solid color-mix(in oklab, var(--brand-SE), white 70%);
    margin-bottom: var(--space-m);
    padding-right: var(--space-2xs);
  }

  .se-tabs__bar::-webkit-scrollbar {
    display: none;
  }

  .se-tab {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.55rem 1rem;
    background: none;
    border: none;
    border-bottom: 3px solid transparent;
    margin-bottom: -2px;
    cursor: pointer;
    font-size: 1rem;
    font-weight: 600;
    color: color-mix(in oklab, var(--color-neutral-700), transparent 20%);
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .se-tab:hover {
    color: var(--brand-SE);
  }

  .se-tab[aria-selected="true"] {
    color: var(--color-neutral-900);
    border-bottom-color: var(--brand-SE);
    font-weight: 700;
  }

  .se-tab [data-icon] {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .se-tab__badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1.4rem;
    height: 1.4rem;
    padding: 0 0.3rem;
    border-radius: 999px;
    background: color-mix(in oklab, var(--brand-SE), white 75%);
    color: color-mix(in oklab, var(--brand-SE), black 15%);
    font-size: 0.75rem;
    font-weight: 700;
    line-height: 1;
  }

  .se-tab[aria-selected="true"] .se-tab__badge {
    background: var(--brand-SE);
    color: #fff;
  }

  /* ── Step List ────────────────────────────────────────────────────────────── */
  .se-step-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .se-step-item {
    display: grid;
    grid-template-columns: 2.5rem minmax(0, 1fr) auto;
    gap: var(--space-s);
    align-items: center;
    padding: var(--space-s) 0;
    border-bottom: 1px dashed color-mix(in oklab, var(--brand-SE), white 65%);
  }

  .se-step-item:last-child {
    border-bottom: none;
  }

  .se-step-num {
    font-size: var(--font-size-1);
    font-weight: 800;
    font-variant-numeric: tabular-nums;
    color: color-mix(in oklab, var(--brand-SE), black 15%);
    letter-spacing: -0.02em;
    line-height: 1;
  }

  .se-step-title {
    margin: 0;
    font-size: var(--font-size-0);
    line-height: 1.4;
    color: var(--foreground-color);
  }

  /* ── Video Preview Card ───────────────────────────────────────────────────── */
  .se-video-preview {
    display: grid;
    grid-template-columns: minmax(140px, 350px) 1fr;
    gap: var(--space-m);
    background: color-mix(in oklab, var(--brand-SE), white 93%);
    border: 1.5px solid color-mix(in oklab, var(--brand-SE), white 72%);
    border-radius: var(--radius-m);
    padding: var(--space-m);
    align-items: center;
    overflow: hidden;
  }

  .se-video-preview__thumb {
    border-radius: var(--radius-s);
    overflow: hidden;
    background: #111;
    flex-shrink: 0;
  }

  .se-video-preview__thumb video {
    width: 100%;
    display: block;
    aspect-ratio: 16 / 9;
    object-fit: cover;
  }

  .se-video-preview__info {
    display: flex;
    flex-direction: column;
    gap: var(--space-s);
  }

  .se-video-preview__title {
    margin: 0;
    font-size: var(--font-size-1);
    font-weight: 700;
    line-height: 1.2;
    color: var(--color-neutral-900);
  }

  .se-video-preview__desc {
    margin: 0;
    font-size: var(--font-size-0);
    line-height: 1.5;
    color: var(--foreground-color);
  }

  .se-video-preview__actions {
    display: flex;
    gap: var(--space-s);
    flex-wrap: wrap;
  }

  .se-divider {
    margin: var(--space-l) 0 var(--space-m);
    display: flex;
    align-items: center;
    gap: var(--space-s);
    font-size: var(--font-size--1);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: color-mix(in oklab, var(--brand-SE), black 20%);
  }

  .se-divider::before,
  .se-divider::after {
    content: '';
    flex: 1;
    border-top: 1px solid var(--brand-SE);
  }

  .se-divider--center {
    justify-content: center;
    color: var(--foreground-color);
  }

  .se-divider--center::before,
  .se-divider--center::after {
    flex: 0.6;
  }

  .se-item-download {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    border: 1px solid color-mix(in oklab,var(--brand-SE),white 45%);
    color: color-mix(in oklab, var(--brand-SE), black 10%);
    background: #fff;
    text-decoration: none;
    border-radius: 10px;
    padding: 0.35rem 0.8rem;
    font-weight: 700;
    font-size: var(--font-size--1);
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .se-item-download:hover {
    background-color: var(--brand-SE);
    color: #fff;
    border-color: color-mix(in oklab, var(--brand-SE), black 6%);
    transform: translateY(-1px);
  }

  .se-module-card {
    border: 2px solid color-mix(in oklab, var(--module-card-color, var(--brand-SE)), white 30%);
    border-radius: var(--radius-l);
    padding: var(--space-m);
    background: color-mix(in oklab, var(--module-card-color, var(--brand-SE)), white 90%);
    box-shadow: var(--elevation-1);
    display: flex;
    flex-direction: column;
    gap: var(--space-s);
    align-content: start;
  }

  .se-module-card h4 {
    color: color-mix(in oklab, var(--module-card-color, var(--brand-SE)), black 2%);
  }

  .se-card-text {
    color: var(--foreground-color);
  }

  .se-card-button {
    width: 100%;
    justify-content: center;
    border-radius: 10px;
    margin-top: auto;
    background: var(--module-card-color, var(--brand-SE));
    color: #fff;
    border: 2px solid color-mix(in oklab,var(--module-card-color, var(--brand-SE)),white 30%);
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .se-card-button:hover {
    background: color-mix(in oklab, var(--module-card-color, var(--brand-SE)), black 10%);
    color: #fff;
    border: 2px solid color-mix(in oklab, var(--module-card-color, var(--brand-SE)), black 10%);
    transform: translateY(-1px);
  }

  @media (max-width: 900px) {
    .se-grid--three {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .se-panel--aim {
      grid-column: 1 / -1;
    }

    .se-module-list {
      --se-part-column: 96px;
    }
  }

  @media (max-width: 700px) {
    .se-grid--three {
      grid-template-columns: 1fr;
    }

    .se-panel--aim {
      grid-column: auto;
    }

    .se-module-row {
      grid-template-columns: 1fr;
      gap: var(--space-xs);
    }

    .se-module-row::after {
      display: none;
    }

    .se-part-button {
      width: fit-content;
      min-width: 7rem;
      justify-self: start;
    }

    .se-teaching-guide {
      flex-direction: column;
      align-items: stretch;
      padding: var(--space-s) var(--space-m);
    }

    .se-package-banner {
      flex-direction: column;
      align-items: stretch;
      padding: var(--space-s) var(--space-m);
    }

    .se-package-banner__title {
      font-size: var(--font-size-0);
    }

    .se-package-btn-wrap {
      align-self: stretch;
    }

    .se-package-btn {
      width: auto;
      justify-content: center;
      white-space: normal;
      text-align: center;
      font-size: 0.9rem;
      display: flex;
      gap: var(--space-s);
      flex-wrap: wrap;
    }

    .se-video-preview__actions {
      flex-direction: column;
    }

    .se-video-action-wrap {
      width: 100%;
    }

    .se-video-action-wrap .se-item-download {
      display: flex;
      gap: var(--space-s);
      flex-wrap: wrap;
      justify-content: center;
    }

    .se-tab {
      padding: 0.5rem 0.7rem;
      font-size: 0.875rem;
      white-space: nowrap;
      gap: 0.3rem;
    }

    .se-tab [data-icon] {
      display: none;
    }

    .se-step-item {
      grid-template-columns: 2rem minmax(0, 1fr) auto;
    }

    .se-video-preview {
      grid-template-columns: 1fr;
    }

    .se-video-preview__title {
      font-size: var(--font-size-0);
    }
  }

  @media (max-width: 480px) {
    .se-tab {
      padding: 0.45rem 0.55rem;
      font-size: 0.70rem;
    }

    .se-tab__badge {
      min-width: 1.2rem;
      height: 1.2rem;
      font-size: 0.7rem;
    }

    .se-step-item {
      grid-template-columns: 2rem 1fr;
      row-gap: var(--space-xs);
    }

    .se-step-item > :last-child {
      grid-column: 2;
      justify-self: start;
    }

    .se-step-title {
      font-size: 0.9rem;
    }
  }

  /* ── Dark mode overrides ─────────────────────────────────────────────────── */
  :global(html.darkmode) .se-panel {
    background: linear-gradient(180deg, var(--dm-surface-2) 0%, var(--dm-surface-1) 100%);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .se-panel__content {
    color: var(--dm-fg-muted);
    border-top-color: var(--dm-border);
  }

  :global(html.darkmode) .se-list li {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-list li + li {
    border-top-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .se-aim-text {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-section__subtitle {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-goal {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-meta-item {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .se-module-row:not(:last-child)::after {
    border-left-color: color-mix(in oklab, var(--brand-SE) 40%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-module {
    background: linear-gradient(155deg, var(--dm-surface-2) 0%, var(--dm-surface-1) 55%);
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .se-module__header h3 a {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .se-module__header h3 a:hover,
  :global(html.darkmode) .se-module__header h3 a:focus-visible {
    color: var(--brand-SE);
  }

  :global(html.darkmode) .se-module__toggle {
    background: color-mix(in oklab, var(--brand-SE) 18%, var(--dm-surface-3));
    border-color: color-mix(in oklab, var(--brand-SE) 35%, var(--dm-border));
    color: var(--brand-SE);
  }

  :global(html.darkmode) .se-module__header:hover .se-module__toggle {
    background: var(--brand-SE);
    border-color: var(--brand-SE);
    color: #1c1917;
  }

  :global(html.darkmode) .se-divider {
    color: color-mix(in oklab, var(--brand-SE), white 20%);
  }

  :global(html.darkmode) .se-divider::before,
  :global(html.darkmode) .se-divider::after {
    border-top-color: color-mix(in oklab, var(--brand-SE) 50%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-divider--center {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-item-download {
    background: var(--dm-surface-1);
    color: color-mix(in oklab, var(--brand-SE), white 25%);
    border-color: color-mix(in oklab, var(--brand-SE) 40%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-item-download:hover {
    background: var(--brand-SE);
    color: #1c1917;
    border-color: var(--brand-SE);
  }

  :global(html.darkmode) .se-module-card {
    background: color-mix(in oklab, var(--module-card-color, var(--brand-SE)) 12%, var(--dm-surface-1));
    border-color: color-mix(in oklab, var(--module-card-color, var(--brand-SE)) 40%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-module-card h4 {
    color: color-mix(in oklab, var(--module-card-color, var(--brand-SE)), white 35%);
  }

  :global(html.darkmode) .se-card-text {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-card-button {
    background: var(--module-card-color, var(--brand-SE));
    border-color: color-mix(in oklab, var(--module-card-color, var(--brand-SE)), black 10%);
    color: #ffffff;
  }

  :global(html.darkmode) .se-card-button:hover {
    background: color-mix(in oklab, var(--module-card-color, var(--brand-SE)), white 15%);
    border-color: var(--module-card-color, var(--brand-SE));
    color: #ffffff;
  }

  :global(html.darkmode) .se-teaching-guide {
    background: color-mix(in oklab, var(--brand-SE) 14%, var(--dm-surface-2));
    border-color: color-mix(in oklab, var(--brand-SE) 35%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-package-banner {
    background: color-mix(in oklab, var(--brand-SE) 14%, var(--dm-surface-2));
    border-color: color-mix(in oklab, var(--brand-SE) 35%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-package-banner__title {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .se-package-banner__sub {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-tabs__bar {
    border-bottom-color: color-mix(in oklab, var(--brand-SE) 30%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-tab {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-tab:hover {
    color: color-mix(in oklab, var(--brand-SE), white 20%);
  }

  :global(html.darkmode) .se-tab[aria-selected="true"] {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .se-tab__badge {
    background: color-mix(in oklab, var(--brand-SE) 25%, var(--dm-surface-3));
    color: color-mix(in oklab, var(--brand-SE), white 30%);
  }

  :global(html.darkmode) .se-tab[aria-selected="true"] .se-tab__badge {
    background: var(--brand-SE);
    color: #1c1917;
  }

  :global(html.darkmode) .se-step-num {
    color: color-mix(in oklab, var(--brand-SE), white 25%);
  }

  :global(html.darkmode) .se-step-title {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-step-item {
    border-bottom-color: color-mix(in oklab, var(--brand-SE) 20%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-video-preview {
    background: color-mix(in oklab, var(--brand-SE) 10%, var(--dm-surface-2));
    border-color: color-mix(in oklab, var(--brand-SE) 28%, var(--dm-border-sub));
  }

  :global(html.darkmode) .se-video-preview__title {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .se-video-preview__desc {
    color: var(--dm-fg-muted);
  }

  /* ── Vertical tabs ──────────────────────────────────────────────────────── */
  .se-tabs--vertical {
    display: grid;
    grid-template-columns: max-content 1fr;
    align-items: start;
    gap: 0;
  }

  .se-tabs--vertical .se-tabs__bar {
    flex-direction: column;
    border-bottom: none;
    border-right: 2px solid color-mix(in oklab, var(--brand-SE), white 70%);
    overflow-x: unset;
    padding-right: 0;
    margin-bottom: 0;
    gap: 10px;
  }

  .se-tabs--vertical .se-tab {
    justify-content: flex-start;
    white-space: nowrap;
    border-bottom-color: transparent;
    border-right: 3px solid transparent;
    margin-bottom: 0;
    margin-right: -2px;
    padding-right: var(--space-l);
    font-size: var(--font-size--3);
    font-weight: 800;
  }

  .se-tabs--vertical .se-tab[aria-selected="true"] {
    border-bottom-color: transparent;
    border-right-color: var(--brand-SE);
  }

  .se-tabs-v__body {
    padding-left: var(--space-l);
  }

  @media (max-width: 640px) {
    .se-tabs--vertical {
      grid-template-columns: 1fr;
    }

    .se-tabs--vertical .se-tabs__bar {
      flex-direction: row;
      border-right: none;
      border-bottom: 2px solid color-mix(in oklab, var(--brand-SE), white 70%);
      overflow-x: auto;
      padding-right: var(--space-2xs);
      margin-bottom: var(--space-m);
    }

    .se-tabs--vertical .se-tab {
      white-space: nowrap;
      border-right-color: transparent;
      border-bottom: 3px solid transparent;
      margin-right: 0;
      margin-bottom: -2px;
      padding-right: 1rem;
    }

    .se-tabs--vertical .se-tab[aria-selected="true"] {
      border-right-color: transparent;
      border-bottom-color: var(--brand-SE);
    }

    .se-tabs-v__body {
      padding-left: 0;
    }
  }

  :global(html.darkmode) .se-tabs--vertical .se-tabs__bar {
    border-right-color: color-mix(in oklab, var(--brand-SE) 30%, var(--dm-border-sub));
  }

  /* ── AOO tab icon + aim panel ───────────────────────────────────────────── */
  .se-tab-img-icon {
    width: 22px;
    height: 22px;
    object-fit: contain;
    flex-shrink: 0;
    border-radius: 50%;
  }

  .se-step-item--plain {
    grid-template-columns: 2.5rem minmax(0, 1fr);
  }

  .se-aim-content {
    display: flex;
    align-items: center;
    gap: var(--space-l);
    padding: var(--space-s) 0;
  }

  /* ── Related Modules compact list ───────────────────────────────────────── */
  .se-rm-list {
    border: 2px solid color-mix(in oklab, var(--color-neutral-500), white 55%);
    border-radius: var(--radius-l);
    overflow: hidden;
  }

  .se-rm-row {
    display: grid;
    grid-template-columns: 56px 1fr auto auto;
    gap: var(--space-m);
    align-items: center;
    padding: var(--space-m) var(--space-l);
    text-decoration: none;
    border-left: 3px solid transparent;
    transition: background 0.15s ease, border-color 0.15s ease;
  }

  .se-rm-row + .se-rm-row {
    border-top: 1px solid color-mix(in oklab, var(--color-neutral-500), white 55%);
  }

  .se-rm-row:hover {
    background: color-mix(in oklab, var(--rm-color, var(--brand-SE)), white 93%);
    border-left-color: var(--rm-color, var(--brand-SE));
  }

  .se-rm-icon {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    overflow: hidden;
    flex-shrink: 0;
  }

  .se-rm-icon img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .se-rm-body {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    min-width: 0;
  }

  .se-rm-title {
    font-size: var(--font-size-0);
    font-weight: 700;
    color: var(--color-neutral-900);
    line-height: 1.2;
  }

  .se-rm-desc {
    font-size: var(--font-size--1);
    color: var(--foreground-color);
    line-height: 1.4;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .se-rm-meta {
    display: flex;
    gap: var(--space-s);
    align-items: center;
    flex-shrink: 0;
  }

  .se-rm-stat {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-size: var(--font-size--1);
    font-weight: 600;
    color: var(--foreground-color);
    white-space: nowrap;
  }

  .se-rm-stat svg {
    width: 1.1rem;
    height: 1.1rem;
    flex-shrink: 0;
    opacity: 0.6;
  }

  .se-rm-row:hover .se-rm-stat svg {
    color: var(--rm-color);
    opacity: 1;
    transition: color 0.2s ease, opacity 0.2s ease;
  }

  .se-rm-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--color-neutral-400);
    transition: color 0.2s ease, transform 0.2s ease;
  }

  .se-rm-arrow svg {
    width: 1.1rem;
    height: 1.1rem;
  }

  .se-rm-row:hover .se-rm-arrow {
    color: var(--rm-color, var(--brand-SE));
    transform: translateX(3px);
  }

  @media (max-width: 640px) {
    .se-rm-row {
      grid-template-columns: 44px 1fr auto;
      gap: var(--space-s);
      padding: var(--space-s) var(--space-m);
    }

    .se-rm-icon {
      width: 44px;
      height: 44px;
    }

    .se-rm-meta {
      display: none;
    }
  }

  :global(html.darkmode) .se-rm-list {
    border-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .se-rm-row + .se-rm-row {
    border-top-color: var(--dm-border-sub);
  }

  :global(html.darkmode) .se-rm-row:hover {
    background: color-mix(in oklab, var(--rm-color, var(--brand-SE)) 12%, var(--dm-surface-2));
  }

  :global(html.darkmode) .se-rm-title {
    color: var(--dm-fg);
  }

  :global(html.darkmode) .se-rm-desc {
    color: var(--dm-fg-muted);
  }

  :global(html.darkmode) .se-rm-stat {
    color: var(--dm-fg-muted);
  }
</style>
